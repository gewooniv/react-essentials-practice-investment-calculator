import Input from "./Input";

export default function Calculator() {
  return (
    <section id="user-input">
      <Input label="Initial investment" initialValue="1000" />
    </section>
  );
}
